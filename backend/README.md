# Dublaja Backend

FastAPI server for text-to-speech, translation, audio dubbing, and character voice effects.

## Run

```bash
poetry install
poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000
```
